import {NavLink,Outlet,useLocation} from 'react-router-dom';import {LayoutDashboard,Building2,FileCheck2,AlertTriangle,Bot,ChevronRight} from 'lucide-react';
const links=[['/','Dashboard',LayoutDashboard],['/empresas','Empresas',Building2],['/certidoes','Certidões',FileCheck2],['/pendencias','Pendências',AlertTriangle],['/automacoes','Automações',Bot]] as const;
export function Layout(){const loc=useLocation();const title=links.find(x=>x[0]===loc.pathname)?.[1]||'ÔMEGA';return <div className="app-shell"><aside className="sidebar"><div className="brand"><div className="brand-mark">Ω</div><div><strong>ÔMEGA</strong><span>GESTÃO EMPRESARIAL</span></div></div><nav className="nav">{links.map(([to,label,Icon])=><NavLink key={to} to={to} className={({isActive})=>`nav-link ${isActive?'active':''}`}><Icon size={17}/><span>{label}</span><ChevronRight size={14} className="nav-arrow"/></NavLink>)}</nav><div className="sidebar-note"><span>Dossiê empresarial</span><strong>Histórico permanente</strong><small>A situação atual é importante. O histórico nunca é perdido.</small></div></aside><main className="main-content"><header className="topbar"><div><span className="eyebrow">PLATAFORMA ÔMEGA</span><h1>{title}</h1></div><div className="api-status"><span className="status-dot"/> Backend conectado</div></header><Outlet/></main></div>}
