import {apiFetch} from './client';import type {Pendencia} from '../../types';export const listarPendencias=(empresaId?:number)=>apiFetch<{total:number;pendencias:Pendencia[]}>(`/api/v1/pendencias${empresaId?`?empresa_id=${empresaId}`:''}`);export const resolverPendencia=(id:number)=>apiFetch<Pendencia>(`/api/v1/pendencias/${id}`,{method:'PATCH',body:JSON.stringify({status:'RESOLVIDA',data_resolucao:new Date().toISOString().slice(0,10)})});
